# Runtime mode for this profile.
OPS_RUNTIME=live
# Chain ID expected from the connected RPC.
CHAIN_ID_EXPECTED=11155111
# RPC endpoint used by Sepolia ops flows.
RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# Private key used to sign Sepolia transactions.
PRIVATE_KEY=${SEPOLIA_PRIVATE_KEY:-${DEFAULT_PRIVATE_KEY:-}}
# Deployer address used when signing happens outside this config.
DEPLOYER=
# Owner address for admin actions and HookFee withdrawals.
OWNER=${SEPOLIA_OWNER:-${DEFAULT_OWNER:-0x96Fa05e991A0d0fcab48a766069db4eC4b8e090A}}

# Runtime controller expectations for the current production-aligned profile.
# *VOLUME values use the internal dollar scale with six implied decimals.
DUST_SWAP_THRESHOLD=4000000
ENTER_CASH_MIN_VOLUME=400000000
ENTER_CASH_EMA_RATIO_PERCENT=135
HOLD_CASH_PERIODS=2
ENTER_EXTREME_MIN_VOLUME=2500000000
ENTER_EXTREME_EMA_RATIO_PERCENT=410
ENTER_EXTREME_CONFIRM_PERIODS=2
HOLD_EXTREME_PERIODS=2
EXIT_EXTREME_EMA_RATIO_PERCENT=120
EXIT_EXTREME_CONFIRM_PERIODS=2
EXIT_CASH_EMA_RATIO_PERCENT=120
EXIT_CASH_CONFIRM_PERIODS=3
LOW_VOLUME_RESET=100000000
LOW_VOLUME_RESET_PERIODS=6

# Minimum ETH balance required before running ops.
BUDGET_MIN_ETH_WEI=100000000000000000
# Minimum stable-token balance required before running ops.
BUDGET_MIN_STABLE_RAW=10000000
# Minimum volatile-token balance required before running ops.
BUDGET_MIN_VOLATILE_RAW=0
# Stable-token amount reserved for liquidity provisioning.
BUDGET_LIQ_STABLE_RAW=20000000
# Volatile-token amount reserved for liquidity provisioning.
BUDGET_LIQ_VOLATILE_RAW=10000000000000000
# Stable-token amount reserved for swap operations.
BUDGET_SWAP_STABLE_RAW=5000000
# Volatile-token amount reserved for swap operations.
BUDGET_SWAP_VOLATILE_RAW=1000000000000000
# Extra ETH buffer reserved above explicit budgets.
BUDGET_SAFETY_BUFFER_ETH_WEI=50000000000000000

# Helper swap driver contract used by live swap flows.
SWAP_DRIVER=
# Helper liquidity driver contract used by live liquidity flows.
LIQUIDITY_DRIVER=

# Stable-token amount used by smoke swaps.
SMOKE_SWAP_STABLE_RAW=1000000
# Stable-token amount used by full validation swaps.
FULL_SWAP_STABLE_RAW=1500000
# Number of swap iterations in the full validation flow.
FULL_SWAP_ITERATIONS=4
# Stable-token amount used by rerun-safe validation swaps.
RERUN_SWAP_STABLE_RAW=800000

# Enables transaction broadcasting for this profile.
OPS_BROADCAST=0
